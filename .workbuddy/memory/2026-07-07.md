# 2026-07-07 工作记录

## 任务：修复 A股结构行情看板「数据缺口：样本覆盖不足」频繁误报

### 根因（已修复）
- `build/src/render.js` 的 `dataQualityGate` 用 `coverageRatio = min(sampleCount/expectedSample, 1)` 比例口径。科技成长 28 只 / expectedSample=48 → ratio=0.58<0.6 → 被误标「样本覆盖不足」并挡出正式主线。
- 修复：coverageRatio 改为**按有效ETF数量分级**（≥5→1.0 / 3–4→0.8 / 1–2→0.4 / 0→0），使 `ratio<0.6 ⟺ 有效<3`。「样本覆盖不足」只在 valid<3 时出现。
- 配套修复：`confidenceLevel` 阈值改 `<COVERAGE_PASS_RATIO`；`buildIndustryEtfBuckets` 去掉 `filter(e=>e.fundSize!=null)`（曾静默丢弃 fundSize 为 null 的占位样本）；`classifyEtf` 的 MAP 改用 6 位代码精确 `CODE_BUCKET`（原 MAP 键带 .SH/.SZ 永不匹配）；`classifyInnovativeDrug` 扩展池加「中药」；02/04 显示串改为 count-based；`STRUCTURE_DEFS` 扩展到 11 个统一桶（新增 医药成长/消费价值/化工/新能源/军工/传媒游戏，化工从有色资源拆出）。

### 数据流关键约束
- HTML 是生成产物：由 `_regen.js` 从 `src/`（normalize/metrics/states/render）+ `theme_etf_data_extended.json`（作为 DASHBOARD_DATA）+ 内联图表函数 bundle 生成。真正改的是 src 模块 + 数据，再跑 regen。
- `normalizeData` 的日期集合只来自 rawEtf/concData 的键，**不含** themeByDate。要给主题数据加新日期（如 07-06）必须同时在 rawData 里克隆最新日作为市场背景，否则该日期被丢弃。
- `_regen.js` 硬编码只读沙箱 PROJ 路径 → 已做本地化副本 `_regen_local.js`（SRC_DIR=build/src，数据=build/data，projCopy 引用 conc/margin/themeBreadth/kline 原始文件，outPath=build/output）。

### 本次产物
- `build/universe_20260706.json`：98 只统一宇宙，11 规范桶，每桶≥3。
- `build/cache/quote.json` + `build/cache/flow.json`：westock 实时拉取 07-06 涨跌/净流入/规模（98 只）。
- `build/build_wide_pool.js`：合并 quote+flow 重建 `data/theme_etf_data_extended.json` 的 20260706 键（11 规范桶 + etfDetails），并克隆 rawData/kline/breadth 的 07-03→07-06 作为市场背景。
- `build/output/fused_dashboard_v2.html`：最终看板（currentDate=2026-07-06）。

### 验收结论（10 项全过）
- 「样本覆盖不足」「数据缺口」三页合计出现 0 次；11 桶 valid 全 ≥3。
- 02 页 11+ 桶 ETF 明细齐全（62 行），创新药核心(8)/扩展(5)分离，微盘用 563300 中证2000ETF 代理。
- 五页渲染零报错，无 NaN/undefined/null/++。
- 01 页 0 个 ETF 代码（仅摘要），与 02 页明细分离。
- 注：正式主线只取 top3（原设计 `formalMainline.slice(0,3)`），07-06 真实行情证券/红利排第4-5被展示位截断，非 bug。

### 待办/注意
- 原项目目录 2026-06-19-22-12-59 与 2026-07-03-11-32-09 为**只读沙箱**，产物留在 `build/output/`。如需替换线上看板需手动拷贝或走部署授权。
- flow 语义已切换为 westock `MainNetFlow`（主力净流入，元→/1e8 亿），与旧净申购额口径不同，主线排名反映真实 07-06 资金。

---

## 任务：审计并要求「所有看板数据必须真实 07-06」（非克隆 07-03）

### 审计发现（07-06 这版最初只有 etfTheme 是真实 07-06 拉的）
- 深比对 07-06 vs 07-03 顶层字段，克隆残留：`etfWide 宽基`(totalFlow=-147.63 与07-03逐字节相同)、`klineSummary`(==07-03)、`market.indices`(chg/flow 与07-03相同)、`margin`(date仍07-03,isStale)、`shareData`(null)。`concentration`/`themeBreadth` 在07-06本就是 empty/partial/null（非克隆但无真实数据）。

### 已修复（全部用 westock 实测 2026-07-06）
- **etfWide 宽基**：拉 55 只宽基 ETF 的 `data_fund_flow`(MainNetFlow) + `data_quote`(chg/规模/成交额)，重算 per-index flow 与 totalFlow。**真实 totalFlow=-6.957亿**（原克隆 -147.63）。脚本 `build_wide_real.js`。
- **market.indices**：拉 6 大指数 `data_quote`+`data_fund_flow`。真实 chg：上证50 +0.91%/沪深300≈0/中证500 -1.08%/中证1000 -1.72%/中证A500 -0.33%/科创50 +1.04%。flow：上证50 -22.96亿/沪深300 -246.88亿/科创50 +16.66亿（指数级主力净流入，westock 有）；中证500/1000/A500 westock 无指数资金流，用对应 ETF 聚合真实值兜底(-0.97/+1.02/-0.58)。脚本 `build_market_real.js`。
- **klineSummary**：拉 32 代码 `data_kline`(limit=25)，重算 chg1d/chg5d/chg20d。31/32 更新（中证2000 sh932000 westock 无数据，保留旧值）。脚本 `build_kline_real.js`，原始结果存于 `~/.workbuddy/projects/.../tool-results/mcp-*-data_kline-*.txt`。
- **idxChgSeries**：用沪深300 真实 chg1d(-0.0035) 回填 idxChg1d。脚本 `build_idx_series.js`。

### 无法还原（Wind 已断开，westock 只给个股级数据，拿不到全市场汇总）
- **margin 两融余额**：`data_fund_margin` 是个股两融明细，非全市场汇总。07-06 仍保留 07-03 快照并标 isStale:true。
- **concentration 成交集中度**(top100个股)：需全市场个股成交额排名，Wind 数据。
- **themeBreadth 主题广度**：需主题成分股涨跌家数，Wind 数据。
- 这三项用户需求"必须真实"但工具不可达，已向用户透明说明，未伪造。

### tdx 探测结论（用户要求"探一下tdx，没有再用wind"）
- 实测 tdx-connector 对这 3 项的能力，**全部拿不到全市场口径**：
  - **margin 全市场两融余额**：`tdx_api_data` margin_trading 是**个股级**（需 code，600519 验证返回 28 行真实两融历史，最新 07-03），无全市场汇总入口；`wenda_macro_query`（宏观两融序列）在当前 defer 索引未注册，调用报 not found。累加 5000+ 个股不现实。→ 拿不到。
  - **concentration 成交集中度**：`tdx_screener` "按成交额排名前100" 返回 100 条，但排序是**代码升序**(601108→601128…)、成交额字段**全 0**，非真实成交额排序；tdx 无全市场成交额快照/排行接口。→ 拿不到可靠值。
  - **themeBreadth 主题广度**：依赖板块成分股列表，但 `tdx_api_data` 板块成分 `tdxf10_bk_cpbd` 返回 **503 S8102 功能未注册**，此环境未开通。无成分股列表则无法算各主题涨跌家数。→ 拿不到。
- **tdx 可用能力确认**：个股级 F10（财务/股东/两融/资金流/龙虎榜）、个股行情(tdx_quotes)、K线(tdx_kline)、条件选股(tdx_screener 但排序不可靠)。对个股分析有用，对"全市场汇总类"复盘字段无能为力。
- **遗留 3 项现状**：看板中 margin(标 isStale:true 07-03 快照)、concentration(全null partial)、themeBreadth(null) 保持诚实空/过期标注，不冒充 07-06。
- **可行替代（待用户确认）**：themeBreadth 可降维为"各主题 ETF 涨跌家数/上涨比例"（已有真实 etfTheme 数据），口径从个股级降到 ETF 级，真实但粒度不同；margin/concentration 无替代。

### 补丁方式说明（重要）
- 未重跑 `_regen_local.js`（会重新克隆 07-03 抹掉刚拉的真实性）。改为直接解析 HTML 中 `var DASHBOARD_DATA = {...}` 整块 → 改对应 entry["2026-07-06"] 的 etfWide/market/klineSummary/idxChgSeries → 整块替换写回 `build/output/fused_dashboard_v2.html`。此方式可复用，避免 regen 回退。
- 验收：五页渲染零报错，NaN/undefined/null/++ 全 0；00 页显示「宽基 -7.0亿」(真)、旧 -148 全网搜不到。

---

## 任务：融资余额用 Wind、主题广度继续成分股涨跌家数（用户明确"不要修改口径"）

### Wind 取数（CLI 调用，非断开）
- Wind MCP skill 走 CLI：`node scripts/cli.mjs call <server> <tool> <params_json>`，**必须用 execFileSync 数组传参**（`execSync` 字符串里单引号 JSON 会被 shell 剥掉 → INVALID_PARAMS_JSON）。
- 融资余额：`economic_data.get_economic_data` 取沪深 M0061606/M0061610，07-06 合计 **29609.76亿=2.9610万亿**，较前日(07-03) chgYi=-104.96亿。无克隆。
- 主题广度：`index_data.get_index_price_indicators` 取 10 主题 Wind 指数成分股涨跌家数（涨/跌/平）。实测 07-06：半导体15/13/1、AI12/38/0、通信6/44/0、机器人48/1/1、创新药37/13/0、证券30/16/3、红利90/10/0、有色金属22/28/0、化工27/22/1、黄金9/40/1。
- 写回脚本：`build/wind_breadth_fetch.js`(拉数存 cache) + `build/build_margin_breadth_patch.js`(写回 HTML DASHBOARD_DATA + projCopy/theme_breadth_data.json)。

### 关键坑：名称映射（子主题名 vs 规范主题名）
- `themeBreadth.themes[].name` 用 **Wind 子主题短名**（半导体/AI/通信/机器人/创新药/证券/红利/有色金属/黄金/化工）；而 02 页卡片 `name` 用 **规范主题名**（科技成长/创新药/医药成长/证券金融/红利防御/有色资源/化工…）。二者不对等，直接 `===` 匹配**永远失败**。
- 修复：加模块级 `BREADTH_ALIAS` 映射（半导体/AI/通信/机器人→科技成长；有色金属/黄金→有色资源；创新药→创新药/医药成长；证券→证券金融；红利→红利防御；化工→化工），在两个消费点按归属表**汇总**（up/down/flat/total 累加，upRatio=Σup/Σtotal）：
  1. `renderThemeCard`（02 页核心指标第5格）：优先显示「主题广度(成分股)」+"(X涨/Y跌/Z平)"，无匹配主题退回「上涨ETF比例」(ETF级)。
  2. `avgEtfUpRatio`（被 `classifyStructureLifecycle`→主线判断/生命周期调用）：按归属表汇总后比对，使主线判断也吃成分股口径；无匹配主题退回 ETF 级。
- 实测：科技成长汇总 81涨/96跌/2平(45%)、有色资源 31涨/68跌/1平(31%) 等全部命中；avgEtfUpRatio 六主题精确命中成分股值。

### 顺手修的 bug
- `renderQuality` 主题ETF 项 `date: etfTheme ? etfTheme.date : '待确认'`：etfTheme 存在但 `.date` 为 null → 页内露出 `📅 数据日期: null`。改为 `etfTheme.date || currentDt`。已消 null。

### 验收（最终）
- 9 页渲染 0 异常；NaN/undefined/null/++ 全 0。
- 02 页：6 张卡显示成分股广度（真值），6 张无数据主题退回 ETF 级。
- margin=2.9610万亿(isStale=false, Wind)；themeBreadth 10 主题全 wind 源；07-06 margin(29610亿)≠07-03(29050亿) 无克隆。

### 交付物
- `build/output/fused_dashboard_v2.html`（currentDate=2026-07-06，已含全部真实补丁）。
- 注：HTML 内联源码与 build/src/render.js 已手工同步（不重跑 regen，避免覆盖 07-06 补丁）。src 额外把 `avgEtfUpRatio` 加入 module.exports 便于测试。

## 任务：修复「成交与宽度 数据缺失」（用户原话：成交与宽度 数据缺失）

### 根因
- **成交缺失**：`entry.concentration.totalAmount` 为 null（本轮新拉），`entry.market` 旧 upRatio=0.688(3804涨/1628跌) 是**假数据**。
- **宽度标记缺失**：topbar meta「市场宽度/主题宽度」靠 `entry.market.breadthSource==='wind'` + `entry.etfTheme.themes.breadthStatus` 判断，本轮宽度用 westock 取、themeBreadth 写在顶层 `entry.themeBreadth`，两处都没接上 → 标记「未采集」。
- **单位错配 bug（重要，历史遗留）**：`fmt()` 在值≥10000 时做 `/10000+'万'` 转换；而 `totalAmount` 单位本来就是**亿**（30911=30911亿=3.09万亿），KPI 又拼 `<span class="unit">亿</span>` → 显示成「3.1万 亿」/「3.1万亿亿」。历史所有 1.6万–3万(亿)日期都中招，只是之前没人细看。

### 修复
- westock `data_market_overview` 取真实值：07-06 全市场成交额 MONEY=**30911.24亿**（前日07-03=31824.54亿、5日均=33315.78亿）；宽度=涨1876/跌3541/平110 → upRatio=**0.3394**。
- 写补丁（`build_amount_breadth_patch.js`）：`concentration.totalAmount=30911.24`、`market.upRatio=0.3394`、`computed.upRatio=0.3394`、`computed.amountChgPct=-2.87%`、`computed.amountVs5d=-7.22%`、`market.breadthStatus='confirmed'`、`computed.breadthProxy=false`。
- 新增 `fmtAmountYi(v)`：≥10000 显「X.XX万亿」、否则「X亿」，专治亿级金额；替换 3 处 `fmt(totalAmount)+'亿'`（00页全市场成交额 / 01页成交水位 / 01页当日成交额）。src + HTML 内联同步。
- topbar meta 判定改为 `breadthStatus==='confirmed'` → 市场宽度/主题宽度显示「2026-07-06」。

### 验收
- 全页（含 renderTopbar）0 异常；NaN/undefined/null/`++` 全 0；单位错配残留 0。
- 成交额显示「3.09万亿」，上涨比例「34%」，meta 标记不再「未采集」。

### 经验教训
- **`fmt` 单位约定**：`fmt()` 默认把数字当「元」做万/亿换算，**不能**直接套在已是「亿」单位的字段（totalAmount）上。亿级金额一律用 `fmtAmountYi`。
- 补真实数据时先核对单位，再核对显示函数，否则会出现「3.1万 亿」这类叠加单位错配。
- meta 标记的判定字段（`breadthSource`/`breadthStatus`）要与实际取数来源一致，否则数据填了标记仍「未采集」。

---

## 任务：消除 07-06 主题ETF资金流克隆（宽基 -148亿·主题 -38.3亿 是 07-03 克隆值）

### 根因（关键架构发现）
- HTML `build/output/fused_dashboard_v2.html` 的 7 个 tab 里，**只有 `tab-structural` 是真正填充的静态 DOM**（`pageHost` 内第一个 `<div class="page tab-content" id="tab-structural">`，约 7.6KB）。其余 6 个（tab-review/tab-structure/tab-capital/tab-etf/tab-quality/tab-validate）都是**空占位符**，`<div class="tab-content" id="tab-xxx"></div>`，由交互 JS 按需点击时从 `DASHBOARD_DATA` 渲染。
- 所以之前"重渲染全部 7 个 tab"的脚本必败：静态页只含 1 个填充 tab，其余 6 个开标签不在 body 里（只在 JS 字符串里），`replaceAllTabs` 要求 7 个同段 → 永远找不到。
- 初始可见的决策卡显示「宽基 -148亿 · 主题 -38.3亿」是**用 07-03 数据预渲染的静态文本**，一直没被重渲染。07-06 的 `etfWide.totalFlow` 其实已是真值 -6.957，只是静态 HTML 没刷新。

### 修复（确定性可复现）
- 数据真值：`build/cache/theme_flow_0706.json`（98 只主题 ETF 的 07-06 份额净申购口径 flow，wind 拉取）。`159995.SZ` 当日 2:1 拆股（份额翻倍、净值减半）被识别为拆分 → flow 归零，避免 +166亿 伪流。
- 脚本 `build/patch_rerender.js`（复用 `src/render.js` 的 `renderStructuralReview`）：解析 HTML 内 `var DASHBOARD_DATA` → patch `etfTheme`（97 只 confirmed + 1 只 split 归零，`date='2026-07-06'`）→ `THEME TOTAL=+57.48亿` → 用**跳过 `<script>` 的 div 栈扫描**定位 `tab-structural` 闭合 `</div>` → 替换静态块 → 同步替换 `DASHBOARD_DATA` JSON 写回。原失败方案（script 分段 + 7-tab 同段定界）已废弃。
- 结果：宽基 **-6.96亿**(真) / 主题 **+57.48亿**(真) → 资金性质判定为「**分歧**」(轮动/切换，非全面撤退)，最大风险「无明显系统性风险」。

### 验收
- `148亿` / `38.3亿` 全文件 **0 处残留**（grep 计数确认）。
- 资金性质链：value=分歧，evidence「宽基 -7.0亿 / 主题 +57.5亿」；最大风险「无明显系统性风险」。
- 剩余 16 处「全面撤退」**全在 JS 条件分支**（`else if(wideNeg && themeNeg) fundingNature='全面撤退'`），仅当宽基且主题同为负才触发；07-06 主题为正，不触发 → 属正常逻辑非显示残留。
- JSON 可解析，文件 2042KB（JSON.stringify 紧凑化，数据无语义丢失）。

### 架构要点（写入 MEMORY.md 长期）
- **改看板数据只改两个地方**：① `DASHBOARD_DATA` 内联 JSON（驱动 6 个 JS 渲染 tab）；② 唯一填充的静态 `tab-structural` 块（首次加载可见）。其余 tab 不碰（空占位符，JS 自动读 JSON）。
- 闭合标签定位统一用"跳过 script 的 div 栈扫描"，不要在 script 分段后假设 tab 同段。

---

## 任务：上涨比口径修正——改用指数成分股上涨占比（非 ETF 池占比）

### 用户纠正
- 用户指出主线评分表「上涨比」应为**指数成分股中上涨占比**，而非我之前解释的「主题 ETF 池 chg>0 占比」。代码里其实有两套：结构页「主题广度(成分股)」用 `themeBreadth`(Wind 成分股家数)，但主线评分表 `buildThemeLeaderBoard` 一直用 `b.upRatio`(ETF 池 chg>0 占比)。

### 根因
- 07-06 的 `entry.themeBreadth` 挂着**历史克隆值**（半导体15/13/1→upRatio0.517、机器人48/1/1→0.960、证券30/16/3→0.612、红利90/10/0→0.900、创新药37/13/0→0.740 等），并非 07-06 当天。之前 inspect 因读嵌套 `t.breadth.upCount` 误判为空，实际是扁平 `t.upRatio` 有值只是旧。
- **与今日日志第71行冲突**：那次记的「实测 07-06 半导体15/13/1…」实为历史日数据被误标 07-06。今日重拉 Wind 才是真 07-06：半导体2/27/0、AI21/29/0、通信18/32/0、机器人15/34/1、创新药3/47/0、证券1/48/0、红利21/79/0、有色金属4/46/0、化工7/41/2、黄金14/36/0。

### 修复
- 拉数：`node build/wind_breadth_fetch.js`（Wind `index_data.get_index_price_indicators` 取 10 主题成分股涨/跌/平家数），真实 07-06 存 `cache/breadth_margin_wind_0706.json`。
- 填数：`build/patch_theme_breadth.js`（WRITE=1）按 name 匹配，用 Wind 真值覆盖 `entry.themeBreadth` 10 子主题 upCount/downCount/flatCount/totalCount/upRatio，date='2026-07-06'。
- 改 `src/render.js`：`buildThemeLeaderBoard` 加 `componentUpRatioByNorm(entry, normName)`（复用 BREADTH_ALIAS 汇总子主题成分股家数→upRatio），在 allBuckets 构建后覆盖 `b.upRatio`；缺失子主题（消费价值/新能源/军工/传媒游戏/微盘）回退 ETF 池。

### 成分股口径下的 07-06 真实排名
| 排名 | 主题 | 成分股upRatio | ETF池upRatio | score | 类型 |
|---|---|---|---|---|---|
|1|科技成长|31%|30%|77|兑现型|
|2|证券金融|2%|86%|69.8|价格扩散型|
|3|红利防御|21%|83%|68.7|价格扩散型|
|4|化工|14%|0%|59.2|兑现型|
|8|创新药/医药成长|6%|77%|52.1|反抽型|

- TOP3 仍是科技/证券/红利（有真实资金流入+价格上涨支撑，不纯靠宽度）；创新药成分股仅6%涨但ETF中位数+1%、当日净流-2.77亿→典型反抽型，#8 不进 TOP3（呼应上轮"为何没创新药"）。
- ETF 池口径系统性高估上涨比（证券86% vs 成分股2%），印证用户纠正正确。

### 验收
- 旧 ETF 池 86%/83%/77% 数据残留 0（唯一 86% 是 CSS color-mix 无关）。
- `themeBreadth` 真实值保留：半导体6.9%/创新药6.0%/证券2.0%。
- `buildThemeLeaderBoard` 经 `analyze_v2.js` 实跑确认返回成分股口径。
- 重渲染 `patch_rerender.js`（WRITE=1）后决策卡/资本/ETF 页一致用成分股口径。
- git commit dd9e7bf；临时导出 buildThemeLeaderBoard/buildInnovativeDrugBucket 已 revert。
